#ifndef MOVIES_H
#define MOVIES_H

#include <string>
#include <utility>

using namespace std;

bool compare(const pair<std::string, double>& a,
                   const pair<std::string, double>& b);

#endif
